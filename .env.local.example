# Used by scripts/fetch-matches.mjs (run server-side, e.g. `npm run fetch:data`),
# not by the browser bundle. Get a free key at
# https://www.football-data.org/client/register
#
# Export it before running the script, e.g.:
#   export FOOTBALL_DATA_API_KEY=your-key-here
#   npm run fetch:data
FOOTBALL_DATA_API_KEY=
