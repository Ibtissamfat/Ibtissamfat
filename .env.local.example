# Copy this file to .env.local and fill in your own key (get one free at
# https://www.football-data.org/client/register). Without it, the app
# falls back to TheSportsDB, then to the bundled demo bracket.
VITE_FOOTBALL_DATA_API_KEY=
